name: Build release archive
on:
  push:
    tags: ['v*']
permissions:
  contents: write
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build archive
        run: |
          chmod +x install.sh uninstall.sh server.py
          mkdir -p dist/NexRelay-sms-control
          cp server.py storage.py install.sh uninstall.sh README.md LICENSE dist/NexRelay-sms-control/
          tar -C dist -czf "dist/NexRelay-sms-control-${GITHUB_REF_NAME}-linux.tar.gz" NexRelay-sms-control
          sha256sum dist/*.tar.gz > dist/SHA256SUMS
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.tar.gz
            dist/SHA256SUMS
